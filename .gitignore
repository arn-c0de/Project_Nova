# ── Project Nova .gitignore ────────────────────────────────────────────────
# Aktuell reines Dokumentationsprojekt (Markdown). Unity-Regeln sind bereits
# vorbereitet, damit ab Sprint 7 (Implementierung) nichts nachgezogen werden muss.

# ── macOS ──────────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
Icon?

# ── Editor / IDE ───────────────────────────────────────────────────────────
.idea/
.vs/
.vscode/
*.swp
*.swo
*~

# ── KI-/Agenten-Tooling (lokal, nicht versioniert) ─────────────────────────
.claude/
.cursor/
.kimi/
reports/          # lokale Orchestrator-/Session-Reports gehören nicht ins Repo

# ── Unity (ab Sprint 7 relevant) ───────────────────────────────────────────
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]serSettings/
[Mm]emoryCaptures/
[Rr]ecordings/
.consulo/
*.tmp
*.unitypackage
*.app
crashlytics-build.properties
ExportedObj/
sysinfo.txt

# Mono-Absturzberichte, die der Editor in den Projektstamm schreibt
mono_crash.*.json

# Unity generierte Solution-/Projektdateien (nur Repo-Root; handgeschriebene
# Tool-Projektdateien wie tools/Nova.SimRunner/Nova.SimRunner.csproj bleiben
# versionierbar)
/*.csproj
*.unityproj
*.sln
*.slnx
*.suo
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# Tools / Build-Outputs (alle .NET-Tool-Projekte)
tools/**/bin/
tools/**/obj/
tools/**/out/

# Unity Asset Meta-Daten von ignorierten Ordnern
/[Aa]ssets/[Aa]ssetStoreTools*
/[Aa]ssets/[Pp]lugins/[Ee]ditor/JetBrains*

# Unity-generiertes Purchasing-Artefakt (wird lokal vom Editor erzeugt)
/[Aa]ssets/Resources/BillingMode.json
/[Aa]ssets/Resources/BillingMode.json.meta

# ── Art-Assets: leben als Paket ausserhalb des Repos ───────────────────────
# Die produzierten 3D-Assets (rund 105 MB) wuerden das Repository mehr als
# verdoppeln und liessen sich spaeter nur per History-Rewrite entfernen. Sie
# werden deshalb als Asset-Paket verteilt: docs/assets/AssetPackage.md.
#
# Ausgeschlossen wird der VOLLSTAENDIGE Art-Inhalt, nicht nur die Binaries:
# bliebe das Prefab im Repo und fehlte sein Mesh, haette ein frischer Clone
# unsichtbare Einheiten statt Graybox-Primitiven. Ohne Prefab faellt
# UnitViewManager sauber auf Primitive zurueck — ein Clone ist damit immer
# spielbar, und ein Paket-Drop-in schaltet die Art per ArtAssetAutoSync frei.
#
# Bewusst NICHT ignoriert: PROVENANCE.json (Lizenz-/Herkunftsnachweis bleibt
# im Repo, auch wenn das Asset es nicht tut), .gitkeep und Ordner-Metas.
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.fbx
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.fbx.meta
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.glb
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.glb.meta
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.gltf
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.gltf.meta
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.png
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.png.meta
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.mat
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.mat.meta
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.prefab
[Aa]ssets/_[Pp]roject/[Aa]rt/**/*.prefab.meta
[Aa]ssets/_[Pp]roject/[Aa]rt/[Ss]ource/

# ── Node / Tooling (falls Build-Skripte hinzukommen) ───────────────────────
node_modules/
npm-debug.log*
.env
.env.local

# Python
__pycache__/
*.pyc

# Player build outputs
Builds/

# Local tool outputs (perf artifacts, generated PDFs) — never gate evidence
output/

# Repo-local .NET SDK
.dotnet/

# ── Sprint 14: Lobby-Laufzeitdateien (D-092/D-094) ─────────────────────────
# lobby-config.json enthält den Supabase-Anon-Key der lokalen Installation
# (Template: lobby-config.example.json — das Beispiel bleibt versioniert).
# NovaBuildCommit.txt stempelt BuildCommitStamp vor jedem Player-Build.
/[Aa]ssets/_Project/Resources/lobby-config.json
/[Aa]ssets/_Project/Resources/lobby-config.json.meta
/[Aa]ssets/_Project/Resources/NovaBuildCommit.txt
/[Aa]ssets/_Project/Resources/NovaBuildCommit.txt.meta
